import React from 'react';
import ReactDOM from 'react-dom';
import App from './screens/FrameChatroom/App.jsx'; // Update the import to point to App.jsx

ReactDOM.render(<App />, document.getElementById('root'));
