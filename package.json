{
    "name": "tree-talk-reactver",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "dependencies": {
        "@parcel/watcher": "^2.4.1",
        "parcel": "^2.12.0"
    },
    "scripts": {
        "build": "cd client && npm install && PUBLIC_URL=./ npm run build && cd ../server && npm install"
    }
}
